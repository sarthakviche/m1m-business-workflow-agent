# ============================================================
# M1M Backend — pinned requirements (TRD Section 2)
# Python 3.11+ required
# ============================================================

# Web framework
fastapi==0.115.5
uvicorn[standard]==0.32.1

# ORM + async DB
sqlalchemy==2.0.36
asyncpg==0.30.0
alembic==1.14.0

# Pydantic (v2) — settings + request/response schemas
pydantic==2.10.3
pydantic-settings==2.6.1

# Auth / JWT
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# LLM — Gemini (primary per TRD)
google-generativeai==0.8.3

# LangGraph agent orchestration
langgraph==0.2.60
langchain-core==0.3.28
langchain-google-genai==2.0.7

# PDF generation
# Version 69.0 ships pre-built wheels for Windows/Linux/macOS — no build tools required.
# Use: pip install weasyprint --only-binary :all:
weasyprint==69.0
jinja2==3.1.4

# CSV / data validation
pandas==2.2.3
openpyxl==3.1.5      # for Excel file support in onboarding

# WhatsApp / HTTP
httpx==0.28.1

# Supabase client (for Storage uploads)
supabase==2.10.0

# Utilities
python-multipart==0.0.18   # for file upload endpoints
python-dotenv==1.0.1

# Testing
pytest==8.3.4
pytest-asyncio==0.24.0
